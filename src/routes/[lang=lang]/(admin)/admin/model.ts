/** Locale-less panel paths; `name` indexes `t.admin` for the card texts. */
export const PATHS = [
	{ href: '/admin/blog', name: 'blog' },
	{ href: '/admin/shop', name: 'shop' },
	{ href: '/admin/bio', name: 'bio' },
	{ href: '/admin/certificates', name: 'certificates' },
	{ href: '/admin/site-settings', name: 'siteSettings' }
] as const;
